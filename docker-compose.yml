services:
  app:
    build: .
    ports:
      - '8001:8001'
    environment:
      TREND_DB_PATH: /app/data/trends.db
      TREND_PORT: 8001
    volumes:
      - trend-data:/app/data

volumes:
  trend-data:
